import React from "react";

const OldCheck = (props) => {
  return (
    <div>
      Date: {props.params.date}
    </div>
  );
// Then you can access the state object from within your component:

// const {foo} = props.location.state

// console.log(foo) // "bar" */}
}

export default OldCheck;
